import React from "react"
import CompassRouter from "./CompassRouter";

// "Connect" creates a component that grabs the store out of state and
//  can map state from the store to properties in a child component
export default CompassRouter;
